# This directory has been created in order to create the tasks for the module "C - Pointers, arrays and strings". 

## Task. 0: 
Create a function that takes a pointer to an int as parameter and updates the value it points to, to 98.
